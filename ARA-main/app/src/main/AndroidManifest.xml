<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31"
        android:theme="@style/Theme.AppCompat">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agregar la página 1 con intent-filter para iniciar la app en esta actividad -->
        <activity
            android:name=".pagina_1"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declarar las actividades pagina_2 a pagina_10 -->
        <activity android:name=".pagina_2" android:exported="true" />
        <activity android:name=".pagina_3" android:exported="true" />
        <activity android:name=".pagina_4" android:exported="true" />
        <activity android:name=".pagina_5" android:exported="true" />
        <activity android:name=".pagina_6" android:exported="true" />
        <activity android:name=".pagina_7" android:exported="true" />
        <activity android:name=".pagina_8" android:exported="true" />
        <activity android:name=".pagina_9" android:exported="true" />
        <activity android:name=".pagina_10" android:exported="true" />

    </application>
</manifest>
